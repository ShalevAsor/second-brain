/**
 * Editor placeholder tips for new notes
 */
export const EDITOR_TIPS = `Start typing your note...

💡 Tips:
- Use \`\`\` for code blocks with syntax highlighting
- Use $$ for LaTeX math equations (inline: $x^2$, block: $$\\frac{a}{b}$$)
- Select text to format (bold, italic, headings, etc.)
- Auto-saves every 5 seconds while you type
- Press Cmd+K for global search`;
